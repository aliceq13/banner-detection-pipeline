# Gamma 붙여넣기 가이드

아래 `=== PASTE START ===` ~ `=== PASTE END ===` 사이를 **전체 복사**해 Gamma의 "텍스트로 만들기(Paste in text)" 입력창에 붙여넣으세요.

**Gamma 생성 옵션 권장값**
- 카드 수: **18장 내외 (Free-form)**
- 언어: 한국어
- 톤: Professional · Technical · Clean
- 대상: 딥러닝 전공 수강생·교수자
- 텍스트 양(Amount of text per card): Medium
- 테마: Minimal (dark accent) 또는 Corporate blue

**Gamma 프롬프트 박스에 추가로 넣을 한 줄 지시문 (선택)**
> "15분 발표용. 전문적·데이터 중심 톤. 슬라이드 제목은 명사 구문. 불릿 ≤5개. 수치는 굵게. 이모지 금지. [FIG ...] 마커 위치에 이미지 placeholder를 삽입."

---

=== PASTE START ===

# 현수막 탐지·보정·분류 멀티모달 파이프라인

세그멘테이션 → 원근 보정 → 멀티모달 VLM 분류

딥러닝 중간고사 프로젝트 · 2026-04

---

## 문제 정의

- 옥외광고물법상 현수막 단속은 **사람 손으로 수행**
- 정당·공공은 합법, 무허가 민간은 단속 대상 → **종류 식별이 관건**
- 현수막은 불규칙 사각형 + 비스듬 촬영 → **bbox만으론 부족**
- 색·로고와 문구 의미가 동시에 필요 → **단일 CNN으로 불충분**

---

## 3-단계 파이프라인

1. **탐지**: 세그멘테이션으로 현수막을 픽셀 단위로 분리
2. **보정**: 다각형에서 네 꼭짓점을 뽑아 원근 왜곡을 정면 시점으로 편다
3. **분류**: 멀티모달 VLM이 문구·로고를 함께 보고 종류를 판별

각 단계는 독립 모듈 → 개별 교체·실험 가능

[FIG: 파이프라인 개요도 · `results/demo/pipeline_demo.png`]

---

## 데이터셋 구성

- **현수막 데이터**: YOLO 세그멘테이션 포맷, 클래스 = banner + text
- **가공**: text 클래스 제거 → banner 단일 클래스 이진 학습셋
- **COCO 음성표본 추가**: 현수막이 전혀 없는 일반 풍경 이미지

| 분할 | Binary (+COCO) | No-COCO | COCO 추가분 |
|---|---:|---:|---:|
| train | **5,881** | 4,281 | 1,600 |
| val   |   734 |   534 |   200 |
| test  |   711 |   511 |   200 |

두 셋은 동일 현수막 이미지를 공유 — **유일 변인은 COCO 포함 여부**

---

## 실험 가설

> **H1**: 현수막이 없는 일반 풍경 이미지를 함께 학습하면,  
> 모델이 배경을 현수막으로 착각하는 빈도가 줄고 Precision이 오른다.

- 두 모델의 하이퍼파라미터 **완전 동일** (YOLO medium, 100 epochs, 입력 640)
- 차이는 오직 **학습 데이터에 COCO 1,600장 포함 여부**
- 평가: (A) 동일 현수막 val 셋 / (B) 현수막 없는 COCO 400장

---

## 결과 1 — 현수막 Val 셋 비교

동일 현수막 val 534장에서 두 모델을 교차 평가

| 지표 | binary2 (+COCO) | no_coco | Δ |
|---|---:|---:|---:|
| mAP@0.5 | 0.9772 | **0.9803** | −0.003 |
| mAP@0.5:0.95 | 0.9327 | **0.9414** | −0.009 |
| **Precision** | **0.9533** | 0.9388 | **+0.015** |
| Recall | 0.9209 | **0.9536** | −0.033 |

현수막 검출력은 거의 동일, **Precision은 binary2가 우위**

[FIG: `banner_binary2/results.png` · `banner_no_coco/results.png` 학습 곡선 병렬]

---

## 결과 2 — 일반 풍경 오탐률 (핵심 결과)

현수막이 전혀 없는 COCO 일반 풍경 400장에서 오탐 측정

| 데이터 | binary2 | no_coco | 배수 |
|---|---:|---:|---:|
| COCO val 200장 | **1 (0.50%)** | 21 (10.50%) | 21× |
| COCO test 200장 | **2 (1.00%)** | 24 (12.00%) | 12× |
| **합계 400장** | **3 (0.75%)** | 45 (11.25%) | **15×** |

no_coco는 **9장 중 1장**에서 배경을 현수막으로 오인

음성표본 학습이 이를 **15배 억제**

---

## 실무적 해석

- 실제 거리 CCTV 영상의 **99% 프레임은 배경**
- 오탐 한 건마다 뒷단(보정 + VLM) 연산이 새로 호출됨
- 오탐 1건당 하류 비용이 큼 → **Precision 우선**이 합리적
- 현수막 mAP를 0.3%p 양보해도 **오탐 15배 감소의 이득이 훨씬 큼**

[FIG: `banner_binary2/BoxPR_curve.png` vs `banner_no_coco/BoxPR_curve.png`]

---

## 원근 보정 — 왜 필요한가

- 거리에서 찍힌 현수막은 **비스듬히 기울어 찍힘**
- VLM이 기울어진 문구를 읽으면 정확도 저하
- 해결: 세그 결과인 **다각형**에서 네 꼭짓점을 잡아 **정면 시점으로 편다**

[FIG: 왜곡 전 / 보정 후 크롭 비교 2~3 쌍]

---

## 네 꼭짓점 선택 방식

다각형의 모든 점 중에서 (x, y)를 조합해 네 극점을 직접 고름

- 좌상 = `x+y` 가 가장 작은 점
- 우상 = `x−y` 가 가장 큰 점
- 우하 = `x+y` 가 가장 큰 점
- 좌하 = `x−y` 가 가장 작은 점

**본 방식의 장점**
- 네 점 대응만으로 호모그래피 해가 유일하게 결정 — **근사 오차 없음**
- 출력 크기를 네 점 간 실측 변 길이로 결정 → **종횡비 보존**

**기존 방식의 한계**
- Convex Hull + 다각형 근사 방식은 **스케일에 민감**
- 근사 파라미터에 따라 꼭짓점 개수가 들쭉날쭉

---

## VLM 분류기 — Qwen3.5-9B

- 9B 파라미터 **이미지+텍스트 입력 / 텍스트 출력** 모델
- 하이브리드 어텐션 구조, 긴 맥락 지원
- 이미지의 **문구·로고·색상을 한 번에 고려**해 정당/민간/공공 3-class 판별
- GPU 메모리 제약을 고려해 **양자화 없이 BF16 원본**으로 로드
- 탐지기는 VLM 로드 전에 내려 **한 GPU에서 순차 실행**

---

## 3단계 구조적 프롬프트

현수막 이미지를 보고 아래 세 단계를 **순서대로** 출력하도록 강제

1. **문구 추출**: 현수막 한글을 그대로 전사
2. **근거 서술**: 문구·로고·색상 기반 2문장 추론
3. **카테고리**: "카테고리: 정당/민간/공공 현수막" 고정 라인

- "문구 추출"은 **모델이 이미지를 제대로 읽는지 검증하는 장치**
- 카테고리 파싱은 여러 단계 폴백으로 견고화

---

## Thinking Mode 활용

- Qwen3 계열은 답변 전에 **내부 추론 과정(think 블록)을 생성**
- 추론 길이가 길어지므로 **생성 토큰 예산을 충분히 확보**
- 최종 답변만 후처리로 분리해 카테고리 파서에 전달
- 큰 이미지에서 출력 품질이 저하되는 이슈가 알려져 있어 **긴 변 896px로 축소**

---

## VLM 평가 설계

- **GT 카테고리가 있는 샘플**을 평가 대상으로 사용
- 한 이미지에 현수막이 여러 장일 수 있어, YOLO 탐지 중 **GT 박스와 IoU ≥ 0.3 매칭된 것**을 골라 원근 보정에 사용
- YOLO가 놓친 경우 → **GT 박스로 직접 잘라 평가 지속** (편향 방지)
- 지표: Accuracy · Precision · Recall · F1 · Confusion Matrix

---

## VLM 평가 결과 (n=10)

| 지표 | 값 |
|---|---|
| **Accuracy** | **80.0% (8/10)** |
| seg 매칭 / crop 대체 / 실패 | 7 / 3 / 0 |
| 공공 현수막 F1 | **1.000** |
| 민간 현수막 F1 | **0.833** |
| 정당 현수막 F1 | 0.000 (샘플 1장, 통계 부족) |

n=10은 **스모크 테스트** — 향후 **n≥50에서 본평가** 예정

[FIG: `results/demo/vlm_confusion_matrix.png`]

---

## 정성 데모

[FIG: `results/demo/pipeline_demo.png` — 6 샘플 × 3 열]

- **열1**: 원본 + GT bbox
- **열2**: 원근 보정 후
- **열3**: VLM 예측 (Pred · Confidence · GT · ✓/✗)

왜곡된 현수막이 **펴진 뒤 정확히 분류되는 흐름**을 한눈에 확인

---

## 창의성·차별점

- **네 꼭짓점 보정**: 현수막 종횡비를 그대로 살린 정면 시점 복원
- **음성표본 학습**: 공개 데이터셋만으로 **일반 풍경 오탐 15× 감소**
- **탐지→보정→분류 계층 분업**: 가벼운 모델과 큰 VLM의 역할 분리
- **3단 구조 프롬프트**: 파싱 안정성 + 이미지 인식 검증 동시 확보

---

## 기술 스택

| 단계 | 기술 |
|---|---|
| 탐지 | YOLO26-seg (ultralytics 2026) |
| 원근 보정 | OpenCV 호모그래피 |
| 분류 | Qwen3.5-9B (멀티모달, BF16) |
| 학습 가속 | AMP (mixed precision) |

- 시드 고정·결정적 실행으로 **재현성 보장**

---

## 한계 및 향후 작업

- **VLM 본평가**: n≥50, 각 class 15+로 confusion matrix 재측정
- **국내 거리 음성표본**: COCO 외 한국 가두 이미지로 도메인 갭 축소
- **지연 시간 벤치**: 실시간성 검증(ms 단위)
- **Confidence 임계값 조정**: Recall 회복 여지 탐색

---

## 핵심 메시지

- 탐지·보정·분류를 **픽셀→기하→의미** 세 층위로 분리한 파이프라인
- **음성표본 학습이 일반 풍경 오탐을 15배 줄인다** — 실무 환경에서 결정적
- 다각형 꼭짓점 기반 보정이 VLM 입력 품질을 끌어올린다
- 멀티모달 VLM이 **문구 + 시각 단서**를 결합해 3-class 분류
- 현 시점 n=10 Accuracy **80%**, n≥50 본평가로 확장 예정

감사합니다.

=== PASTE END ===
