# ============================================================
# requirements.txt - Python 패키지 목록
# ============================================================
# 설치: pip install -r requirements.txt
# ============================================================

# ── YOLO26 객체 탐지 / 세그멘테이션 ───────────────────────
# Ultralytics: YOLO 시리즈 공식 패키지 (YOLO26 포함)
ultralytics>=8.3.0

# ── VLM (Qwen3.5-9B 등) 추론 ──────────────────────────────
# HuggingFace Transformers: 사전학습 모델 로드
transformers>=4.47.0
# 모델 양자화 (4-bit, 8-bit) - 옵션 (config.VLM_CONFIG["quantization"])
bitsandbytes>=0.44.0
# 가속화 라이브러리 (device_map, dtype 처리 등)
accelerate>=1.0.0
# SentencePiece: 토크나이저 의존성 (Qwen/Gemma 계열 공통)
sentencepiece>=0.2.0
# Protobuf: 모델 직렬화
protobuf>=3.20.0

# ── 이미지 처리 ───────────────────────────────────────────
# OpenCV: H-matrix 계산, 이미지 변환
opencv-python>=4.8.0
# Pillow: 이미지 로드/저장
Pillow>=10.0.0
# NumPy: 수치 연산
numpy>=1.24.0

# ── COCO 데이터셋 처리 ────────────────────────────────────
# pycocotools: COCO 포맷 파싱 및 평가 지표
pycocotools>=2.0.7

# ── 학습 / 평가 유틸리티 ─────────────────────────────────
# scikit-learn: 분류 평가 지표 (F1, confusion matrix 등)
scikit-learn>=1.3.0
# matplotlib: 결과 시각화
matplotlib>=3.7.0
# seaborn: 고급 시각화 (confusion matrix 등)
seaborn>=0.13.0
# tqdm: 진행바 표시
tqdm>=4.65.0
# pandas: 결과 데이터 정리
pandas>=2.0.0

# ── 개발 도구 ─────────────────────────────────────────────
# PyYAML: YAML 설정 파일 파싱
PyYAML>=6.0
# requests: HTTP 요청 (COCO 다운로드 등)
requests>=2.31.0
# huggingface_hub: 모델 다운로드
huggingface_hub>=0.20.0
