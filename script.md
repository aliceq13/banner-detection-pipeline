# 발표 대본 (약 10분)

- 톤: 존댓말, 담담하지만 수치는 강조
- 총 슬라이드: 19장 · 목표 시간 10:00
- 괄호 `(…)`는 숨 고르는 포즈 또는 제스처 지시입니다
- **굵게** 처리된 부분은 강세를 주세요

---

## 슬라이드 1 — 타이틀 (0:00 – 0:20)

> 안녕하세요. "**현수막 탐지·보정·분류 멀티모달 파이프라인**"이라는 주제로 발표하겠습니다. YOLO 세그멘테이션, 호모그래피 원근 보정, 그리고 Qwen 3.5 멀티모달 모델을 이어 붙인 **3-단계 구조**입니다. (…) 시작하겠습니다.

---

## 슬라이드 2 — 문제 정의 (0:20 – 1:00)

> 먼저 왜 이 문제를 풀려고 했는지 말씀드리면, 옥외광고물법상 불법 현수막 단속은 **현재 사람이 일일이 확인하는 구조**입니다. 법상으로 정당·공공 현수막은 합법, 무허가 민간 현수막은 단속 대상이기 때문에 **"종류를 식별하는 것"이 자동화의 핵심**입니다.
>
> 그런데 현수막은 보통 비스듬히 찍히고 사각형이 불규칙하기 때문에 단순히 bbox만 쳐서는 부족하고, 색·로고·문구 의미를 **동시에** 봐야 하므로 단일 CNN만으로는 어렵다고 판단했습니다. 그래서 **세 단계를 나눠 접근**했습니다.

---

## 슬라이드 3 — 3-단계 파이프라인 (1:00 – 1:45)

> 전체 구조입니다. (다이어그램을 가리키며)
>
> **1단계 탐지**는 YOLO26-seg로, 현수막을 픽셀 단위 다각형으로 분리합니다. **2단계 보정**은 그 다각형에서 네 꼭짓점을 뽑아 호모그래피로 원근 왜곡을 펴서 정면 시점의 패치를 만듭니다. **3단계 분류**는 Qwen3.5-9B 멀티모달 VLM이 그 패치를 보고 정당·민간·공공 중 하나로 분류합니다.
>
> 세 단계를 **독립 모듈로 설계**해서, 어느 한 단계만 교체하거나 실험하기 쉽게 만든 것이 구조적 포인트입니다.

---

## 슬라이드 4 — 데이터셋 구성 (1:45 – 2:15)

> 데이터는 두 가지를 사용했습니다. **현수막 데이터**는 YOLO 세그멘테이션 포맷으로 이미 라벨링된 공개 셋이고요. 여기에 제가 추가한 게 **COCO 음성표본**입니다. (…) 음성표본이란 "현수막이 **전혀 없는**" 일반 풍경 이미지로, 라벨 파일은 비어 있습니다.
>
> 테이블 보시면 COCO 포함 셋이 train 5,881장, 미포함 셋이 4,281장이고, **현수막 이미지는 완전히 동일하게 공유**합니다. **유일한 변인은 COCO 1,600장의 포함 여부**입니다. 이 설계 덕분에 뒤에서 깔끔한 Ablation이 가능합니다.

---

## 슬라이드 5 — 실험 가설 (2:15 – 2:45)

> 이 구성으로 검증한 가설은 하나입니다. "**일반 풍경 음성표본을 함께 학습하면, 모델이 배경을 현수막으로 착각하는 경우가 줄어든다**."
>
> 확인 방법은 두 갈래로 나눴습니다. (A) 동일한 현수막 val 셋에서 두 모델을 교차 평가하고, (B) **현수막이 전혀 없는 COCO 400장**을 별도로 돌려 오탐률을 측정했습니다.

---

## 슬라이드 6 — 결과 1: 현수막 Val 비교 (2:45 – 3:20)

> 첫 번째 결과입니다. 동일한 현수막 val 셋 534장에서 두 모델의 성능은 거의 비슷합니다. mAP@0.5 기준 0.9772 대 0.9803으로 **차이가 0.3%p 수준**입니다.
>
> 다만 주목할 수치는 **Precision**입니다. COCO 음성표본을 넣은 모델이 **1.5%p 더 높습니다.** 현수막 검출력은 유지하면서 "확신할 때만 탐지"하는 방향으로 **더 보수적으로 학습된** 결과입니다.

---

## 슬라이드 7 — 결과 2: 일반 풍경 오탐률 (3:20 – 4:20) ★

> 다음이 제가 생각하는 **핵심 결과**입니다. 현수막이 **전혀 없는** COCO 일반 풍경 이미지 400장을 양쪽 모델에 돌렸습니다.
>
> 음성표본을 학습하지 않은 모델은 **9장 중 1장에서 배경을 현수막으로 환각**합니다. 반면 COCO를 함께 학습한 모델은 **100장에 1장도 안 되는 수준**으로 내려가서, **오탐률이 15배 감소**했습니다. (…)
>
> 수치로 11.25% → 0.75%. 이게 본 프로젝트에서 제가 가장 확실하게 보여드리고 싶은 결과입니다.

---

## 슬라이드 8 — 실무적 해석 (4:20 – 4:50)

> 이 수치가 왜 중요한지 한 번 더 짚어 드리면, 실제 거리 CCTV 영상에서는 **프레임의 99%가 배경**입니다. 그리고 **오탐이 한 번 발생할 때마다** 뒷단의 원근 보정과 18GB짜리 VLM이 새로 호출됩니다.
>
> 그래서 현수막 자체의 mAP를 **0.3%p** 양보하더라도, 오탐 15배 감소의 이득이 **훨씬 크다**는 것이 제 결론입니다. 실전 배포를 염두에 두면 Precision 우선 설계가 맞다고 판단했습니다.

---

## 슬라이드 9 — 원근 보정: 왜 필요한가 (4:50 – 5:20)

> 다음은 2단계, 원근 보정입니다. 거리에서 찍힌 현수막은 **비스듬히 찍혀 있기 때문에** VLM이 기울어진 글자를 읽으면 정확도가 떨어집니다. 그래서 탐지 결과인 다각형에서 **네 꼭짓점을 잡아 정면 시점으로 펴는 작업**이 필요합니다. (그림 가리키며) 왼쪽 왜곡된 상태에서 오른쪽 정면으로 복원하는 흐름입니다.

---

## 슬라이드 10 — 네 꼭짓점 선택 방식 (5:20 – 6:00)

> 네 꼭짓점을 뽑는 방법은 굉장히 단순하게 설계했습니다. 다각형의 **모든 점** 중에서, x+y가 가장 작은 점을 좌상, x−y가 가장 큰 점을 우상 — 이런 식으로 **네 방향의 극값을 취해서** 네 꼭짓점을 확정합니다.
>
> 이 방식의 장점은 세 가지입니다. 첫째, **네 점 대응만으로 호모그래피 해가 유일하게 결정되기 때문에** 근사 오차가 없고 RANSAC 같은 반복 추정도 필요 없습니다. 둘째, 출력 크기를 **네 점 사이 실측 변 길이**로 정하기 때문에 **현수막 본래의 종횡비가 보존**됩니다. 셋째, Convex Hull이나 다각형 근사처럼 파라미터에 민감한 단계가 없어서 **불규칙한 마스크에도 안정적**입니다.

---

## 슬라이드 11 — VLM 분류기: Qwen3.5-9B (6:00 – 6:40)

> 3단계 분류기로는 **Qwen3.5-9B**를 선택했습니다. 90억 파라미터의 **이미지+텍스트 입력, 텍스트 출력** 멀티모달 모델입니다. 하이브리드 어텐션 구조에 긴 맥락을 지원합니다.
>
> 색·로고·문구를 **한 번에 고려**해서 판단할 수 있다는 게 단일 CNN 대비 결정적인 장점입니다. GPU 메모리는 **BF16 원본으로 약 18GB**를 사용하고, 탐지기를 먼저 내리고 VLM을 올리는 **순차 실행** 방식으로 한 GPU에서 돌립니다.

---

## 슬라이드 12 — 3단계 구조적 프롬프트 (6:40 – 7:20)

> 프롬프트는 세 단계로 **순서를 강제**했습니다. 첫째, 현수막 한글을 **그대로 전사**하게 합니다. 둘째, 추출한 문구와 로고·색상을 근거로 두 문장 추론합니다. 셋째, "카테고리: 정당 / 민간 / 공공 현수막" 고정 라인을 출력합니다.
>
> 첫 단계 "문구 추출"이 특별히 중요한데, 이건 **모델이 이미지를 제대로 읽고 있는지 검증하는 장치**입니다. VLM이 문구를 틀리면 분류도 안 맞을 테니까요. 카테고리 파싱은 **다단계 폴백**을 두어 포맷이 일부 깨져도 결과를 복원할 수 있게 했습니다.

---

## 슬라이드 13 — Thinking Mode (7:20 – 7:45)

> Qwen3 계열의 특징인 **Thinking Mode**도 켰습니다. 모델이 최종 답변 전에 **내부 추론 과정을 먼저 생성**하는 기능입니다. 추론 품질을 올리는 대신 토큰 예산을 충분히 잡아야 해서, 최종 답변만 후처리로 분리해 파서에 넘겼습니다. 또 큰 이미지에서 출력 품질이 저하되는 이슈가 알려져 있어서 **입력을 896픽셀로 축소**했습니다.

---

## 슬라이드 14 — VLM 평가 설계 (7:45 – 8:15)

> 정량 평가는 다음과 같이 설계했습니다. 먼저 **GT 카테고리가 있는 샘플**만 추려서 평가 대상으로 삼았습니다. 한 이미지에 현수막이 여러 장 있을 수 있기 때문에, YOLO가 찾은 탐지 결과 중 **GT 박스와 IoU 0.3 이상 겹치는 것**을 "이 GT의 짝"으로 보고 그 다각형을 보정에 썼습니다.
>
> YOLO가 아예 못 찾은 샘플을 버리면 **쉬운 현수막만 제외되는 편향**이 생기기 때문에, 이 경우에는 **GT 박스로 직접 크롭해서 평가를 이어 갔습니다**. 이렇게 하면 YOLO 성능과 VLM 성능을 분리해서 볼 수 있습니다. 최종 지표는 **Accuracy, Precision, Recall, F1, Confusion Matrix**입니다.

---

## 슬라이드 15 — VLM 평가 결과 (8:15 – 8:50)

> 결과입니다. **정확도 80%**가 나왔고 10장 중 8장을 맞혔습니다. 공공 현수막은 F1이 **1.0**으로 완벽히 맞혔고, 민간은 **0.83**이었습니다. 정당은 샘플이 1장뿐이어서 통계적으로 의미가 없는 0.0이 찍혔는데, 이 부분은 n≥50으로 **본평가를 별도로 진행할 예정**입니다. 현재는 파이프라인이 끝까지 돌아간다는 것과 대략적인 상한을 확인한 **스모크 테스트**로 보시면 됩니다.

---

## 슬라이드 16 — 정성 데모 (8:50 – 9:10)

> 정성 결과 한 장 보여 드리겠습니다. (그림 가리키며) 왼쪽 열이 원본과 GT bbox, 가운데 열이 원근 보정 후, 오른쪽 열이 VLM 예측입니다. **왜곡되어 있던 현수막이 펴진 뒤 정확히 분류되는 흐름**을 한눈에 확인하실 수 있습니다.

---

## 슬라이드 17 — 창의성·차별점 (9:10 – 9:40)

> 요약하면 본 프로젝트의 차별점은 네 가지입니다.
>
> **첫째, 네 꼭짓점 기반 호모그래피**로 현수막 종횡비를 살렸고, **둘째, 공개 데이터셋만으로 오탐을 15배 줄였으며, 셋째,** 가벼운 탐지기와 큰 VLM의 **역할을 분리**하는 계층 구조를 설계했고, **넷째, 3단 구조 프롬프트**로 파싱 안정성과 OCR 검증을 동시에 확보했습니다.

---

## 슬라이드 18 — 기술 스택 (9:40 – 9:50)

> 사용 기술은 YOLO26-seg, OpenCV, Qwen3.5-9B, AMP mixed precision이며 모든 학습은 시드 고정으로 **재현성을 보장**했습니다.

---

## 슬라이드 19 — 향후 작업 + 마무리 (9:50 – 10:00)

> 향후로는 VLM 본평가를 n≥50 규모로 재측정하고, 한국 거리 음성표본으로 도메인 갭을 좁히려고 합니다.
>
> (…) 이상으로 발표를 마치겠습니다. **감사합니다.**

---

## 발표 팁

- **슬라이드 7 (FPR 15×)** 에서 한 호흡 쉬세요. 가장 강조해야 할 수치입니다.
- **슬라이드 10 (네 꼭짓점)** 은 수식 대신 손짓으로 "좌상·우상·우하·좌하"를 짚는 게 훨씬 잘 전달됩니다.
- **슬라이드 15 (정당 F1=0)** 은 방어적으로 설명하지 말고, "스모크 테스트, 본평가 예정"으로 **담담하게** 넘기세요.
- 질문 예상: "왜 Qwen3.5-9B인가?" → 이미지+텍스트 입력을 한 번에 받는 모델 중 24GB에 올릴 수 있는 최대 성능급.
- 질문 예상: "왜 RANSAC 안 썼는가?" → 네 점 대응이면 정확해가 존재해서 반복 추정이 불필요.
